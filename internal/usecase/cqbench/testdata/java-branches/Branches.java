public class Branches {
    int pick(boolean flag) {
        int result;
        if (flag) {
            result = compute();
        } else {
            result = compute();
        }
        return result;
    }

    int compute() {
        return 42;
    }
}
